for(int i = 0; i < length; i++)
{
	some_function(a[i]) // A + Bi
}

notes = &recorded_notes

while(user doesnt hit enter) {
	if(valid note) {

		*notes = note // store note into the address
		notes++       // move to next note
	}
}